{
  "devDependencies": {
    "@types/node": "^20.10.6"
  },
  "scripts": {
    "build": "concurrently \"cd backend && npm install && tsc\" \"cd frontend && npm install && npm run build\"",
    "start": "concurrently \"node backend/server.js\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "concurrently": "^6.2.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "react-bootstrap-icons": "^1.10.3",
    "typescript": "^5.4.5"
  }
}
